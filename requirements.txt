# Production dependencies
# Each teammate adds their own as they implement assigned issues.
#
# Jerry   -> core/agent.py, core/memory/, core/retrieval/, core/context/, core/llm/
# Kelechi -> core/db/, deployment, CI hardening
# Sarah   -> ui/, evaluation/, demo scripts

# Jerry: background Leiden community detection over the typed graph
# (core/memory/community.py). Lazy-imported, with a connected-components fallback.
leidenalg>=0.10
igraph>=0.11

# Kelechi: Slack bot with Socket Mode (slack/bot.py)
slack-bolt>=1.18
python-dotenv>=1.0

# Sarah: Streamlit demo UI shell (ui/app.py)
streamlit>=1.36

# Jerry: FastAPI product backend adapter (api/main.py)
fastapi>=0.111
uvicorn[standard]>=0.30
