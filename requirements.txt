# Production dependencies
# Production dependencies grouped by subsystem.

# Jerry: background Leiden community detection over the typed graph
# (core/memory/community.py). Lazy-imported, with a connected-components fallback.
leidenalg>=0.10
igraph>=0.11
networkx>=3.0
openai>=1.0
chromadb>=0.5
fastembed>=0.4

# Kelechi: Slack bot with Socket Mode (slack/bot.py)
slack-bolt>=1.18
python-dotenv>=1.0

# Streamlit demo UI shell (ui/app.py)
streamlit>=1.36

# Jerry: FastAPI product backend adapter (api/main.py)
fastapi>=0.111
uvicorn[standard]>=0.30
httpx>=0.27
python-multipart>=0.0.9
# Official Model Context Protocol SDK. Keep the stable v1 line until v2 is released.
mcp>=1.28,<2
